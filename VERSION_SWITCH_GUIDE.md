# 条件配置版本切换指南

## 如何切换到新版本

### 方法1：通过界面按钮切换

1. **进入规则配置页面**
   - 访问任意规则的配置页面
   - 例如：`http://localhost:8080/rule/config/123`

2. **点击升级按钮**
   - 在页面右上角找到 "🧪 升级到新版本" 按钮
   - 点击按钮后会出现确认对话框
   - 确认后自动跳转到新版本

3. **体验新版本功能**
   - 多层条件构建器
   - 任意层级且或关系组合
   - 可视化条件测试

### 方法2：通过URL参数切换

直接在URL中添加 `version=v2` 参数：

```
# 传统版本
http://localhost:8080/rule/config/123

# 新版本
http://localhost:8080/rule/config/123?version=v2
```

## 如何切换回传统版本

### 从新版本切换回传统版本

1. **点击传统版本按钮**
   - 在新版本页面右上角找到 "↩️ 传统版本" 按钮
   - 点击确认后自动回到传统版本

2. **或者移除URL参数**
   - 手动移除URL中的 `?version=v2` 参数
   - 刷新页面即可

## 版本对比

### 传统版本特点
- ✅ 稳定可靠，已验证
- ✅ 简单易用
- ❌ 只支持2层条件结构
- ❌ 第一层只能OR，第二层只能AND

### 新版本特点
- ✅ 支持任意层级嵌套
- ✅ 每层都可以独立设置AND/OR
- ✅ 可视化树形条件构建
- ✅ 在线条件测试功能
- ✅ 向后兼容传统版本数据
- ⚠️ 新功能，建议先测试

## 数据兼容性

### 数据安全保证
- ✅ **完全向后兼容**：传统版本数据在新版本中完全可用
- ✅ **数据不丢失**：切换版本不会导致数据丢失
- ✅ **随时切换**：可以随时在两个版本间切换

### 数据迁移
- 传统版本 → 新版本：自动迁移，无需手动操作
- 新版本 → 传统版本：复杂条件会简化为兼容结构

## 推荐使用场景

### 推荐使用新版本的场景
- 复杂业务规则需求
- 需要多层嵌套条件
- 需要灵活的且或关系组合
- 需要可视化条件构建

### 推荐使用传统版本的场景
- 简单的2层条件已满足需求
- 对稳定性要求极高的生产环境
- 用户习惯传统界面操作

## 功能对比示例

### 传统版本只能表达：
```
条件集 (固定OR关系)
├── 条件组1 (固定AND关系)
│   ├── 用户年龄 > 18
│   └── 用户性别 = 男
└── 条件组2 (固定AND关系)
    ├── 用户等级 = VIP
    └── 订单金额 > 1000
```

### 新版本可以表达：
```
条件集 (可选OR/AND)
├── 条件组A (可选OR/AND)
│   ├── 用户年龄 > 18
│   └── 条件组B (可选OR/AND)
│       ├── 用户性别 = 男
│       └── 用户等级 = VIP
└── 条件组C (可选OR/AND)
    ├── 订单金额 > 1000
    └── 是否新用户 = true
```

## 常见问题

### Q: 切换版本会影响现有数据吗？
A: 不会。两个版本使用相同的数据源，切换版本不会影响数据。

### Q: 新版本的性能如何？
A: 新版本在复杂条件场景下性能更优，简单条件与传统版本性能相当。

### Q: 如何确定使用哪个版本？
A: 如果现有功能满足需求，可继续使用传统版本；如需复杂条件逻辑，建议使用新版本。

### Q: 新版本有什么风险？
A: 新版本经过充分测试，但建议在测试环境先验证后再在生产环境使用。

## 反馈和支持

如果在使用过程中遇到问题：

1. **功能问题**：可通过新版本的"测试条件"功能验证逻辑
2. **界面问题**：可随时切换回传统版本
3. **数据问题**：联系技术支持团队

---

**最后更新时间：2024-01-19**